FROM php:fpm-alpine
LABEL authors="rencema"

RUN set -ex \
    && apk --no-cache add postgresql-dev postgresql-libs \
    && docker-php-ext-install pdo pgsql pdo_pgsql \
    && docker-php-ext-enable pdo_pgsql \
    && apk del postgresql-dev
